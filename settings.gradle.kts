pluginManagement {
    repositories {
        gradlePluginPortal()
        // paperweight-userdev is published to the PaperMC repo, not the Gradle plugin portal.
        maven("https://repo.papermc.io/repository/maven-public/")
    }
}

rootProject.name = "SkJadePlus"
